---
phase: 03-security-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [SDOC-01, SDOC-02, SDOC-03]

must_haves:
  truths:
    - "A Security section exists in README.md between Quick Start and SQL query tracking"
    - "Security section has four subsections in order: Kill Switch, Allowed Hosts, Header Redaction, Database File"
    - "A [!WARNING] callout appears in Quick Start after pip install but before code examples"
    - "The warning links to the Security section via anchor"
    - "The Environment variable section is consolidated into the Security section"
  artifacts:
    - path: "README.md"
      provides: "Security documentation and production warning"
      contains: "## Security"
  key_links:
    - from: "Quick Start [!WARNING]"
      to: "## Security"
      via: "anchor link #security"
      pattern: "\\[Security\\]\\(#security\\)"
---

<objective>
Write the Security section in README.md and add a production warning callout to Quick Start.

Purpose: Users understand FlowSurgeon is a development tool and know how to keep it safe (allowed_hosts, header redaction, kill switch, .gitignore).
Output: Updated README.md with Security section and [!WARNING] callout.
</objective>

<execution_context>
@/home/voidp/.claude/get-shit-done/workflows/execute-plan.md
@/home/voidp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-security-documentation/03-CONTEXT.md
@.planning/phases/03-security-documentation/03-RESEARCH.md

@README.md
@src/flowsurgeon/core/config.py

<interfaces>
<!-- Source of truth for all field names and defaults — from config.py -->

Config fields relevant to Security section:
- enabled: bool — default False, env var FLOWSURGEON_ENABLED (accepts "1", "true", "yes" case-insensitive)
- allowed_hosts: list[str] — default ["127.0.0.1", "::1", "localhost"]
- strip_sensitive_headers: list[str] — default ["authorization", "cookie", "set-cookie"]
- db_path: str — default "flowsurgeon.db"

README structure (current line numbers):
- Line 36: ## Installation (pip install block)
- Line 48: ## Quick start
- Line 91: (end of Flask example)
- Line 92: ## SQL query tracking
- Line 131: ## Configuration
- Line 240: ## Environment variable
- Line 249: ## License
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Security section and [!WARNING] callout to README.md</name>
  <files>README.md</files>
  <action>
Make two edits to README.md:

**Edit 1 — [!WARNING] callout in Quick Start (SDOC-02):**
Insert a GitHub-flavored Markdown warning alert immediately after the Installation section's closing line (line 46: "Requires Python 3.12+...") and before `## Quick start` (line 48). The callout goes between Installation and Quick Start so it is seen right after install:

```markdown
> [!WARNING]
> FlowSurgeon is a development tool. Do not enable in production. See [Security](#security) for details.
```

**Edit 2 — Security section (SDOC-01 + SDOC-03):**
Insert a new `## Security` section between Quick Start (ends around line 91 after Flask example) and `## SQL query tracking` (currently line 92). The section has four subsections in risk-priority order per locked decision:

1. **Kill Switch** — explain `enabled` defaults to False, show env var approach (`FLOWSURGEON_ENABLED=1 uvicorn myapp:app`) and code approach (`Config(enabled=True)`). One sentence why: the middleware is a no-op unless explicitly enabled.

2. **Allowed Hosts** — explain only listed IPs can access the debug panel. State defaults: `["127.0.0.1", "::1", "localhost"]`. Show customization: `Config(allowed_hosts=["127.0.0.1", "::1", "localhost", "192.168.1.50"])`.

3. **Header Redaction** — explain headers are replaced with "[redacted]" before storage. Field name is `strip_sensitive_headers` (NOT `redact_headers`). Defaults: `["authorization", "cookie", "set-cookie"]` (lowercase). Show customization adding `"x-api-key"`.

4. **Database File** — explain FlowSurgeon writes request data to a local SQLite file (`flowsurgeon.db` by default). Provide ready-to-copy .gitignore entry. Phrase as "Add to your project's `.gitignore`" (not the FlowSurgeon repo's .gitignore).

Each subsection follows the locked pattern: one sentence context/why, then a code block showing defaults + customization. Keep each subsection ~5 lines + code block.

Use `from flowsurgeon import Config` in Python code blocks. Use lowercase header values in strip_sensitive_headers (matching config.py source of truth).

**Edit 3 — Consolidate Environment variable section:**
The existing "## Environment variable" section (lines 240-248) overlaps with the Kill Switch subsection. Replace it with a short one-liner redirect:

```markdown
## Environment variable

See [Security](#security) for the `FLOWSURGEON_ENABLED` kill switch and other security settings.
```

This preserves the heading (in case external links reference it) while avoiding content duplication.

**Tone per locked decision:** Friendly but clear, informative not scary. "Do this" examples only — no anti-pattern examples. Brief "why" + "how" for each topic.
  </action>
  <verify>
    <automated>grep -c "## Security" README.md && grep -c "\[!WARNING\]" README.md && grep -c "strip_sensitive_headers" README.md && grep -c "flowsurgeon.db" README.md && uv run pytest tests/ -x -q</automated>
  </verify>
  <done>
    - README.md contains a ## Security section with four subsections (Kill Switch, Allowed Hosts, Header Redaction, Database File) positioned between Quick Start and SQL query tracking
    - A [!WARNING] callout appears between Installation and Quick Start with production warning text linking to #security
    - .gitignore guidance for flowsurgeon.db is present in the Database File subsection
    - All field names match config.py (strip_sensitive_headers, allowed_hosts, enabled)
    - Environment variable section is a one-liner redirect to Security
    - Existing test suite still passes (no code changes, but confirms no accidental breakage)
  </done>
</task>

</tasks>

<verification>
- `grep "## Security" README.md` returns a match
- `grep "\[!WARNING\]" README.md` returns a match
- `grep "strip_sensitive_headers" README.md` appears in the Security section (not just Configuration)
- `grep "flowsurgeon.db" README.md` appears in the Database File subsection
- The Security section appears after `## Quick start` and before `## SQL query tracking` in the file
- `uv run pytest tests/ -x -q` passes (regression check)
</verification>

<success_criteria>
README.md contains all three SDOC requirements: a Security section documenting allowed_hosts, header redaction, and kill switch (SDOC-01); a [!WARNING] production warning callout (SDOC-02); and .gitignore guidance for flowsurgeon.db (SDOC-03). All config field names and default values match src/flowsurgeon/core/config.py exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-security-documentation/03-01-SUMMARY.md`
</output>
