---
phase: 04-announcement-assets
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - README.md
  - docs/announcements/reddit.md
  - docs/announcements/x-thread.md
  - docs/announcements/hacker-news.md
autonomous: true
requirements: [ANMT-01, ANMT-02]

must_haves:
  truths:
    - "README hero image shows the new routes home page screenshot from repo-relative path"
    - "README Debug UI section includes the SQL tab screenshot and demo GIF"
    - "No dev.to S3 URLs remain anywhere in README.md"
    - "Three announcement post drafts exist ready for copy-paste"
  artifacts:
    - path: "README.md"
      provides: "Updated README with repo-relative image paths"
      contains: "docs/screenshots/routes-home.png"
    - path: "docs/announcements/reddit.md"
      provides: "Reddit announcement post draft"
      min_lines: 20
    - path: "docs/announcements/x-thread.md"
      provides: "X/Twitter thread draft"
      min_lines: 10
    - path: "docs/announcements/hacker-news.md"
      provides: "Hacker News Show HN submission draft"
      min_lines: 5
  key_links:
    - from: "README.md"
      to: "docs/screenshots/routes-home.png"
      via: "img src attribute"
      pattern: 'src="docs/screenshots/routes-home.png"'
    - from: "README.md"
      to: "docs/screenshots/request-detail-sql.png"
      via: "img src attribute"
      pattern: 'src="docs/screenshots/request-detail-sql.png"'
    - from: "README.md"
      to: "docs/demo/demo.gif"
      via: "img src attribute"
      pattern: 'src="docs/demo/demo.gif"'
---

<objective>
Update README.md to use the new repo-relative screenshots and GIF, remove all dev.to external image references, and write three platform-specific announcement post drafts.

Purpose: The README needs fresh, repo-hosted visual assets for credible public launch. Announcement drafts save time on launch day — ready to copy-paste.
Output: Updated `README.md`, `docs/announcements/reddit.md`, `docs/announcements/x-thread.md`, `docs/announcements/hacker-news.md`
</objective>

<execution_context>
@/home/voidp/.claude/get-shit-done/workflows/execute-plan.md
@/home/voidp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-announcement-assets/04-CONTEXT.md
@.planning/phases/04-announcement-assets/04-RESEARCH.md
@.planning/phases/04-announcement-assets/04-01-SUMMARY.md

@README.md

<interfaces>
<!-- Assets produced by Plan 01, available at these paths -->
docs/screenshots/routes-home.png    — Hero screenshot (routes home, dark theme, realistic data)
docs/screenshots/request-detail-sql.png — Detail screenshot (SQL tab with slow/dup badges)
docs/demo/demo.gif                  — Walkthrough GIF (routes home -> route -> detail -> SQL tab)
src/flowsurgeon/ui/assets/logo_flowsurgeon.png — Existing logo file in the package
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with repo-relative images</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — understand current structure, find all dev.to URLs)
  </read_first>
  <action>
Make these specific edits to README.md:

**1. Line 2 — Logo image:**
Replace the dev.to logo URL. The logo exists at `src/flowsurgeon/ui/assets/logo_flowsurgeon.png`. Copy it to `docs/screenshots/logo.png` for a clean repo-relative path, then update line 2:
```html
<!-- BEFORE -->
<img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/4ulii4nulch2u8qopdec.png" alt="FlowSurgeon" width="96">
<!-- AFTER -->
<img src="docs/screenshots/logo.png" alt="FlowSurgeon" width="96">
```

**2. Lines 17-19 — Hero screenshot:**
Replace the dev.to hero image with the routes home screenshot:
```html
<!-- BEFORE -->
<div align="center">
    <img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/ztt5hfvreqd7xff0jymz.png" alt="Home Screen">
</div>
<!-- AFTER -->
<div align="center">
    <img src="docs/screenshots/routes-home.png" alt="FlowSurgeon routes home page" width="900">
</div>
```

**3. After the "Request detail -- four tabs" section (after line 277):**
Add the SQL tab screenshot and demo GIF in the Debug UI section. Insert after the Profile tab description paragraph (line 298 area, after the `**ASGI note:**` paragraph):

```html

<div align="center">
    <img src="docs/screenshots/request-detail-sql.png" alt="Request detail — SQL tab with slow and duplicate query badges" width="900">
</div>

<div align="center">
    <img src="docs/demo/demo.gif" alt="FlowSurgeon demo walkthrough" width="900">
</div>
```

**4. Final sweep:** Search the entire README for any remaining `dev-to-uploads.s3.amazonaws.com` URLs and remove/replace them all. There should be exactly 2 (lines 2 and 18) — both handled above.

**5. Copy logo:** `cp src/flowsurgeon/ui/assets/logo_flowsurgeon.png docs/screenshots/logo.png`

Do NOT add any new README sections, comparison tables, or TL;DR blocks (per user decision).
  </action>
  <verify>
    <automated>python -c "content = open('README.md').read(); assert 'dev-to-uploads.s3.amazonaws.com' not in content, 'dev.to URLs still present'; assert 'docs/screenshots/routes-home.png' in content, 'hero screenshot missing'; assert 'docs/screenshots/request-detail-sql.png' in content, 'detail screenshot missing'; assert 'docs/demo/demo.gif' in content, 'demo GIF missing'; assert 'docs/screenshots/logo.png' in content, 'logo missing'; print('README updated correctly')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'dev-to-uploads' README.md` returns 0 (no dev.to URLs remain)
    - `grep -q 'docs/screenshots/routes-home.png' README.md` succeeds
    - `grep -q 'docs/screenshots/request-detail-sql.png' README.md` succeeds
    - `grep -q 'docs/demo/demo.gif' README.md` succeeds
    - `grep -q 'docs/screenshots/logo.png' README.md` succeeds
    - `test -f docs/screenshots/logo.png` succeeds (logo copied)
    - No new sections added (section count unchanged or minimal)
  </acceptance_criteria>
  <done>README.md references only repo-relative image paths. Zero dev.to URLs remain. Hero screenshot, detail screenshot, and demo GIF are all embedded.</done>
</task>

<task type="auto">
  <name>Task 2: Write announcement post drafts</name>
  <files>docs/announcements/reddit.md, docs/announcements/x-thread.md, docs/announcements/hacker-news.md</files>
  <read_first>
    - README.md (updated version from Task 1 — extract Features list, Quick Start code, and description for post content)
    - .planning/phases/04-announcement-assets/04-CONTEXT.md (announcement framing decisions)
  </read_first>
  <action>
Create `docs/announcements/` directory and write three announcement drafts.

**Primary pitch (per user decision):** "django-debug-toolbar for everyone" — framework-agnostic alternative for Python web devs.

**Key selling points to weave in:**
- Zero application changes — wraps any WSGI/ASGI app with one line
- Auto-detects Flask/FastAPI
- SQL query tracking with slow/duplicate detection
- Built-in dark-themed debug UI at `/flowsurgeon`
- Route auto-discovery
- cProfile integration
- No build step, no React, no npm

**1. `docs/announcements/reddit.md`:**
- Title: `Show r/Python: FlowSurgeon -- django-debug-toolbar for Flask & FastAPI`
- Format: long-form Reddit post (300-500 words)
- Structure: hook (problem) -> solution (what FlowSurgeon does) -> quick-start code block (3 lines for FastAPI) -> screenshot/GIF references (as GitHub image URLs using `https://raw.githubusercontent.com/vpcraft/flowsurgeon/master/docs/screenshots/routes-home.png`) -> link to PyPI and GitHub
- Tone: genuine, dev-to-dev, not marketing-speak
- Include pip install command: `pip install flowsurgeon`
- Mention it's MIT licensed and open source
- Note at top: suggested subreddits (r/Python, r/flask, r/FastAPI)

**2. `docs/announcements/x-thread.md`:**
- Format: X/Twitter thread (4-5 tweets)
- Tweet 1: Hook + screenshot reference ("django-debug-toolbar for everyone")
- Tweet 2: One-liner install + 3-line code snippet
- Tweet 3: Key features (SQL tracking, slow/dup detection)
- Tweet 4: GIF walkthrough reference + GitHub link
- Each tweet under 280 characters
- Use line breaks between tweets for readability

**3. `docs/announcements/hacker-news.md`:**
- Title: `Show HN: FlowSurgeon -- Debug toolbar for Flask and FastAPI (Python)`
- Format: HN-style concise description (2-3 sentences max in body)
- Plain text only (HN does not render markdown)
- Link: GitHub repo URL
- Body: what it does, why it exists (no DDT for FastAPI/Flask without framework-specific plugins), how it works (WSGI/ASGI middleware)

All posts should reference the GitHub repo URL: `https://github.com/vpcraft/flowsurgeon`
  </action>
  <verify>
    <automated>python -c "import os; assert os.path.exists('docs/announcements/reddit.md'), 'reddit.md missing'; assert os.path.exists('docs/announcements/x-thread.md'), 'x-thread.md missing'; assert os.path.exists('docs/announcements/hacker-news.md'), 'hacker-news.md missing'; print('All announcement drafts exist')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/announcements/reddit.md` succeeds
    - `test -f docs/announcements/x-thread.md` succeeds
    - `test -f docs/announcements/hacker-news.md` succeeds
    - `grep -q 'django-debug-toolbar' docs/announcements/reddit.md` succeeds (primary pitch present)
    - `grep -q 'github.com/vpcraft/flowsurgeon' docs/announcements/reddit.md` succeeds (repo link)
    - `grep -q 'Show HN' docs/announcements/hacker-news.md` succeeds
    - `wc -l docs/announcements/reddit.md | awk '{print $1}'` returns >= 20 (substantive post)
  </acceptance_criteria>
  <done>Three announcement post drafts exist in docs/announcements/, each tailored to its platform's conventions. All reference the "django-debug-toolbar for everyone" pitch and link to the GitHub repo.</done>
</task>

</tasks>

<verification>
- README has zero dev.to external image URLs
- README references all three repo-relative assets (2 screenshots + 1 GIF)
- Three announcement draft files exist with platform-appropriate content
- Existing test suite still passes: `uv run --group dev pytest tests/ -x -q`
</verification>

<success_criteria>
- `python -c "assert 'dev-to-uploads' not in open('README.md').read()"` passes
- `python -c "assert 'docs/screenshots/routes-home.png' in open('README.md').read()"` passes
- All three announcement files exist and are non-empty
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/04-announcement-assets/04-02-SUMMARY.md`
</output>
