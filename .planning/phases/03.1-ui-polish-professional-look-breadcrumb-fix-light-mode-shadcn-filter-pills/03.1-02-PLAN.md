---
phase: 03.1-ui-polish
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - src/flowsurgeon/ui/templates/home.html
  - src/flowsurgeon/ui/templates/detail.html
autonomous: false
requirements:
  - UIPOL-BREADCRUMB
  - UIPOL-TOGGLE-MARKUP
  - UIPOL-LOGO

must_haves:
  truths:
    - "FlowSurgeon logo in topbar is a clickable link to home on every page"
    - "No duplicate FlowSurgeon text appears in detail page breadcrumb"
    - "Breadcrumb separator is forward slash / not chevron"
    - "Filter controls are a shadcn-style toggle group, not loose pill buttons"
    - "Route count is right-aligned on same row as toggle group"
  artifacts:
    - path: "src/flowsurgeon/ui/templates/home.html"
      provides: "Logo link, toggle group markup, route count in controls-row"
      contains: "toggle-group"
    - path: "src/flowsurgeon/ui/templates/detail.html"
      provides: "Fixed breadcrumb with logo link, no duplicate text, / separator"
      contains: '<a class="logo"'
  key_links:
    - from: "home.html toggle-group markup"
      to: "base.html .toggle-group CSS"
      via: "CSS class reference"
      pattern: 'class="toggle-group"'
    - from: "home.html toggle-item :class binding"
      to: "Alpine.js methodFilter state"
      via: "x-data scope in .spa-wrapper"
      pattern: "methodFilter === "
    - from: "detail.html breadcrumb b-mid link"
      to: "home page filtered view"
      via: "href with method+path query params"
      pattern: 'debug_route.*method=.*path='
---

<objective>
Fix template markup in home.html and detail.html: make logo a clickable link, replace filter pills with toggle group markup, fix breadcrumb structure (remove duplicate text, change separator to /).

Purpose: Complete the UI polish by applying the markup that references the CSS classes created in Plan 01.
Output: Updated home.html and detail.html with professional navigation and controls.
</objective>

<execution_context>
@/home/voidp/.claude/get-shit-done/workflows/execute-plan.md
@/home/voidp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-ui-polish-professional-look-breadcrumb-fix-light-mode-shadcn-filter-pills/03.1-CONTEXT.md
@.planning/phases/03.1-ui-polish-professional-look-breadcrumb-fix-light-mode-shadcn-filter-pills/03.1-UI-SPEC.md
@.planning/phases/03.1-ui-polish-professional-look-breadcrumb-fix-light-mode-shadcn-filter-pills/03.1-01-SUMMARY.md

<interfaces>
<!-- CSS classes available from Plan 01 (base.html) -->
.toggle-group — outer container: inline-flex, bordered, rounded
.toggle-item — individual button: transparent bg, dim text, sans font
.toggle-item:hover — elevated bg, body text color
.toggle-item-active — elevated bg, primary text, font-weight 600
.controls-row — already exists: flex, space-between, center-aligned

<!-- Existing classes used in markup -->
.logo — font-size 20px, font-weight 700, color var(--text-brand)
.breadcrumb — flex, center, gap 8px, font-size 14px
.breadcrumb .sep — color var(--text-faint)
.breadcrumb .b-mid — color var(--text-body), font-weight 500
.breadcrumb .b-cur — color var(--text-brand), font-weight 500
.method .m-GET etc — method badges (existing)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix home.html — logo link, toggle group, route count</name>
  <files>src/flowsurgeon/ui/templates/home.html</files>
  <read_first>
    - src/flowsurgeon/ui/templates/home.html (current markup to replace)
    - src/flowsurgeon/ui/templates/base.html (verify toggle-group CSS classes exist from Plan 01)
  </read_first>
  <action>
Replace the entire `{% block topbar %}` section in home.html with:

```html
{% block topbar %}
<nav class="topbar">
  <a class="logo" href="{{ debug_route }}">FlowSurgeon</a>
</nav>
{% endblock %}
```

Key change: `<span class="logo">` becomes `<a class="logo" href="{{ debug_route }}">`.

Replace the filter pills block AND the route count block (currently lines 14-29) with a single controls-row:

```html
  {# -- Method filter toggle group -- #}
  <div class="controls-row" style="flex-shrink: 0;">
    <div class="toggle-group">
      {% for m in ['ALL', 'GET', 'POST', 'PUT', 'DELETE', 'PATCH'] %}
      <button
        class="toggle-item"
        :class="methodFilter === '{{ m }}' ? 'toggle-item-active' : ''"
        @click="methodFilter = '{{ m }}'"
        type="button"
      >{{ m }}</button>
      {% endfor %}
    </div>
    <span class="u-dim u-text-sm">{{ total_routes }} route{% if total_routes != 1 %}s{% endif %}</span>
  </div>
```

This replaces TWO existing blocks:
1. The `.filter-pills` div (lines 14-24)
2. The route count div (lines 26-29)

Both are merged into one `.controls-row` with toggle group left-aligned and route count right-aligned (via `justify-content: space-between` from existing `.controls-row` CSS).

CRITICAL: The toggle group markup MUST remain inside the `<div class="spa-wrapper" x-data="{ methodFilter: 'ALL' }">` wrapper (line 12) so Alpine.js `:class` bindings work. Place it as the first child of `.spa-wrapper`, same position as the old `.filter-pills`.
  </action>
  <verify>
    <automated>cd /home/voidp/Projects/oss/flowsurgeon && grep -q '<a class="logo" href="{{ debug_route }}">FlowSurgeon</a>' src/flowsurgeon/ui/templates/home.html && grep -q 'class="toggle-group"' src/flowsurgeon/ui/templates/home.html && grep -q 'class="toggle-item"' src/flowsurgeon/ui/templates/home.html && grep -q 'toggle-item-active' src/flowsurgeon/ui/templates/home.html && grep -q 'class="controls-row"' src/flowsurgeon/ui/templates/home.html && grep -c "filter-pills\|pill-active\|class=\"pill\"" src/flowsurgeon/ui/templates/home.html | grep -q "^0$" && grep -c '<span class="logo">' src/flowsurgeon/ui/templates/home.html | grep -q "^0$" && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - home.html contains `<a class="logo" href="{{ debug_route }}">FlowSurgeon</a>`
    - home.html does NOT contain `<span class="logo">`
    - home.html contains `class="toggle-group"`
    - home.html contains `class="toggle-item"`
    - home.html contains `toggle-item-active`
    - home.html contains `class="controls-row"`
    - home.html does NOT contain `filter-pills`
    - home.html does NOT contain `class="pill"`
    - home.html does NOT contain `pill-active`
    - Toggle group is inside `x-data="{ methodFilter: 'ALL' }"` scope (inside .spa-wrapper)
    - Route count span and toggle group are siblings inside same `.controls-row` div
    - `python -m pytest tests/ -x -q` passes
  </acceptance_criteria>
  <done>Home page logo is a clickable link, filter pills replaced with shadcn-style toggle group inside controls-row, route count right-aligned on same row, all within Alpine.js x-data scope. Tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Fix detail.html breadcrumb — remove duplicate, change separator</name>
  <files>src/flowsurgeon/ui/templates/detail.html</files>
  <read_first>
    - src/flowsurgeon/ui/templates/detail.html (current breadcrumb markup to fix)
  </read_first>
  <action>
Replace the entire `{% block topbar %}` section in detail.html (currently lines 7-21) with:

```html
{% block topbar %}
<nav class="topbar">
  <a class="logo" href="{{ debug_route }}">FlowSurgeon</a>
  <div class="breadcrumb">
    <span class="sep">/</span>
    <a class="b-mid" href="{{ debug_route }}?method={{ record.method }}&amp;path={{ record.path | urlencode }}">
      <span class="method {{ record.method | method_class }}">{{ record.method }}</span>
      <span class="u-mono u-text-sm">{{ record.path }}</span>
    </a>
    <span class="sep">/</span>
    <span class="b-cur">{{ record.request_id[:8] }}</span>
  </div>
</nav>
{% endblock %}
```

Changes from current markup:
1. `<span class="logo">FlowSurgeon</span>` becomes `<a class="logo" href="{{ debug_route }}">FlowSurgeon</a>` — logo is now a link
2. Remove `<a class="b-dim" href="{{ debug_route }}">FlowSurgeon</a>` from inside `.breadcrumb` — this was the duplicate "FlowSurgeon" text
3. Change both `<span class="sep">&rsaquo;</span>` to `<span class="sep">/</span>` — forward slash separator per locked decision
4. Breadcrumb now starts with separator `/` then route-level link, then `/` then request ID — the "FlowSurgeon" brand is ONLY in the logo, not repeated in breadcrumb
  </action>
  <verify>
    <automated>cd /home/voidp/Projects/oss/flowsurgeon && grep -q '<a class="logo" href="{{ debug_route }}">FlowSurgeon</a>' src/flowsurgeon/ui/templates/detail.html && grep -c '<span class="logo">' src/flowsurgeon/ui/templates/detail.html | grep -q "^0$" && grep -c 'b-dim' src/flowsurgeon/ui/templates/detail.html | grep -q "^0$" && grep -c '&rsaquo;' src/flowsurgeon/ui/templates/detail.html | grep -q "^0$" && grep -c '<span class="sep">/</span>' src/flowsurgeon/ui/templates/detail.html | grep -q "^2$" && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - detail.html contains `<a class="logo" href="{{ debug_route }}">FlowSurgeon</a>`
    - detail.html does NOT contain `<span class="logo">`
    - detail.html does NOT contain `b-dim` (duplicate FlowSurgeon link removed)
    - detail.html does NOT contain `&rsaquo;` (chevron separator gone)
    - detail.html contains exactly 2 instances of `<span class="sep">/</span>` (two forward slash separators)
    - Breadcrumb structure is: logo / [method badge + path link] / [request_id]
    - `python -m pytest tests/ -x -q` passes
  </acceptance_criteria>
  <done>Detail page breadcrumb fixed: single logo link (no duplicate), forward slash separators, three-level hierarchy (logo / route / request). Tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification in browser</name>
  <files>src/flowsurgeon/ui/templates/base.html, src/flowsurgeon/ui/templates/home.html, src/flowsurgeon/ui/templates/detail.html</files>
  <action>
Human visual verification checkpoint. All automated work is complete from Tasks 1-2 and Plan 01.

What was built: Complete UI polish — animation removal, Swagger-tight spacing, toggle group filter controls, breadcrumb fix, and light mode support across all three template files.

Steps to verify:
1. Start the dev server and visit the FlowSurgeon debug UI home page
2. Verify: page loads instantly (no fade/slide animations)
3. Verify: route list rows are noticeably denser/tighter than before
4. Verify: filter controls are a bordered toggle group (not loose colored pills)
5. Click "GET" in toggle group — verify it gets active styling (elevated bg, bold text, NOT green)
6. Verify: route count shows right-aligned on same row as toggle group
7. Verify: FlowSurgeon logo in topbar is a clickable link
8. Click a route row to go to request detail page
9. Verify: breadcrumb shows `FlowSurgeon / [GET] /path / abc12345` with forward slash separators
10. Verify: NO duplicate "FlowSurgeon" text in breadcrumb area
11. Toggle your OS to light mode (System Settings > Appearance > Light)
12. Verify: background becomes white, text becomes dark, method badges retain Swagger colors
13. Toggle back to dark mode — verify dark theme restores

Resume signal: Type "approved" or describe any visual issues.
  </action>
  <verify>
    <automated>cd /home/voidp/Projects/oss/flowsurgeon && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>User has visually verified: no animations, dense rows, toggle group works, breadcrumb correct, light mode functional. Approved.</done>
</task>

</tasks>

<verification>
After all tasks:
1. Logo is `<a>` link on both home.html and detail.html
2. No `<span class="logo">` remains in any template
3. No `b-dim` class in detail.html breadcrumb
4. No `&rsaquo;` in detail.html — only `/` separators
5. Toggle group markup in home.html with correct Alpine.js bindings
6. No `filter-pills`, `.pill`, `.pill-active` in home.html
7. Route count and toggle group in same `.controls-row`
8. `python -m pytest tests/ -x -q` passes
</verification>

<success_criteria>
- Home page has clickable logo link
- Home page filter is toggle group (not pills)
- Route count is right-aligned on toggle group row
- Detail page breadcrumb: logo / route / request (no duplicate, / separator)
- Both templates reference CSS classes from Plan 01
- Visual checkpoint approved by user
- Existing pytest suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ui-polish-professional-look-breadcrumb-fix-light-mode-shadcn-filter-pills/03.1-02-SUMMARY.md`
</output>
